# HH_FMOD repo ignores — FMOD Studio per‑user and scratch data

# Ignore FMOD per-user settings (safe to ignore)
*.user

# Ignore any '.unsaved' folder created by FMOD Studio (at any depth)
.unsaved/
**/.unsaved/
**/.unsaved/**

# 資料夾（任意層級）
.cache/
**/.cache/**
_cache/
**/_cache/**
.unsaved/
**/.unsaved/**

# 快取檔
**/*.pdc
**/buildrecords/**
**/fsbcache/**
**/*.fobj
**/*.br

# (Optional) If you ever need to version a specific .user file or an .unsaved subpath,
# use a negation rule below, e.g.:
# !path/to/keep/MySettings.user
